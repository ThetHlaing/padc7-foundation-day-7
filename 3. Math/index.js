//Ref: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math

function getRandomIntInclusive(min, max) {    
    //Round up the min variable with ceiling
    //Round up the max variable with floor    
    //Must return values between min and maximum (inclusives)    
    return randomValue; 
  }

  console.log(getRandomIntInclusive(0,5)); //Result should be between 0 to 5
